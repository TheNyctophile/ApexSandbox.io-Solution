public Decimal computeTip(Decimal total, Integer percent) {
    decimal tip; //VARIABLE TO BE RETURNED

    //CHECK IF THE PERCENTAGE LIES BETWEEN 0 TO 100
    if(percent >= 0 && percent <= 100) {
        tip = (total * decimal.valueOf(percent))/100;
    }    
    return tip;
}