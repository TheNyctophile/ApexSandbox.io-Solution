public String changeTimeFormat(String strTime) {
    String hour, minutes, timeVal;
    hour = strTime.substringBefore(':');
    minute = strTime.substringAfter(':');

    Integer hr = Integer.valueOf(hour);
    Integer min = Integer.valueOf(minute);

    timeVal = String.valueOf(hr) + ':' + String.valueOf(min);
}