public String changeTimeFormat(String strTime) {
    String hours, minutes, timeVal;
    hours = strTime.substringBefore(':');
    minutes = strTime.substringAfter(':');

    Integer hr = Integer.valueOf(hours);
    Integer min = Integer.valueOf(minutes);

    timeVal = hours + ':' + minutes;
    return timeVal;
}