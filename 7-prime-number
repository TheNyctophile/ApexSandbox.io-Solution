public Boolean isPrime(Integer num) {
    boolean result;
    integer count =0;
    for(Integer n = 2; n <= num; n++) {
        if(math.mod(num, n) == 0) {
            count ++;
        }
    }
    result = (count > 2) ? false : true;
    return result;
}