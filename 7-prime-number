public Boolean isPrime(Integer num) {
    boolean result;
    for(Integer n = 2; n <= num-1; n++) {
        if(math.mod(num, n) == 0 && math.mod(num, 1) == 0) {
            result = true;
        }
        else result = false;
     }
     return result;
}