public Boolean isPrime(Integer num) {
    Boolean result;
    if(num > 1) {
        for(Integer n = 2; n == num-1; n++) {
            if(math.mod(num, 1)==0 && math.mod(num, n)== 0) {
                result = true;
            }
            else result = false;
        }
    }
}