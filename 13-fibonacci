public Integer fibonacci(Integer n) {
    Integer firstNum, secondNum, addn, count = 1;
    firstNum = 0;
    secondNum = 1;
    
    while(count == n) {
        addn = firstNum + secondNum;
        firstNum = secondNum;
        secondNum = addn;
        count++;
    }

    return addn;
}