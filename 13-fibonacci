public Integer fibonacci(Integer n) {
    Integer firstNum = 1, secondNum = 1, addn, count = 1;
    
    while(count <= n) {
        addn = firstNum + secondNum;
        firstNum = secondNum;
        secondNum = addn;
        count++;
    }

    return addn;
}