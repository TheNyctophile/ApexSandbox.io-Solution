public Integer fibonacci(Integer n) {
    Integer firstNum, secondNum, addn, count;
    
    while(count == n) {
        firstNum = 0;
        secondNum = 1;
        count = 1;
        addn = firstNum + secondNum;
        firstNum = secondNum;
        secondNum = addn;
        count++;
    }

    return addn;
}